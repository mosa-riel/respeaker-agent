# ESPHome secrets for the reSpeaker satellite.
# Copy to the ESPHome add-on as `secrets.yaml` (HA: /config/esphome/secrets.yaml).
# NEVER commit the real file — it holds the wifi + OTA passwords.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
ota_password: "GENERATE_A_RANDOM_STRING"
